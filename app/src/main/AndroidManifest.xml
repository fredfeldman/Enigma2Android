<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android tablet — touchscreen is required -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Enigma2Android"
        android:usesCleartextTraffic="true">

        <!-- Main launcher activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Full-screen video player activity -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.Enigma2Android.Player"
            android:launchMode="singleTop" />

        <!-- Settings activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- Hidden bouquets picker (launched from Settings) -->
        <activity
            android:name=".ui.settings.HideBouquetsActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- EPG Import (launched from Settings if plugin is present) -->
        <activity
            android:name=".ui.settings.EpgImportActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- Receiver Box Info (launched from Settings) -->
        <activity
            android:name=".ui.settings.BoxInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- Bouquet Editor (launched from Settings) -->
        <activity
            android:name=".ui.bouqueteditor.BouquetEditorActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- Receiver Settings (launched from Settings) -->
        <activity
            android:name=".ui.receiversettings.ReceiverSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- v1.0.7: Virtual Remote Control -->
        <activity
            android:name=".ui.remote.RemoteControlActivity"
            android:exported="false"
            android:label="@string/remote_title"
            android:theme="@style/Theme.Enigma2Android.Settings" />

        <!-- v1.0.7: Quick Settings tile (toggle standby) -->
        <service
            android:name=".service.PowerToggleTileService"
            android:label="@string/tile_power_label"
            android:icon="@android:drawable/ic_lock_power_off"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
